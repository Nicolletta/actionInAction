name: Hello World

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World with UTC time
        run: echo "$(date -u '+%Y-%m-%dT%H:%M:%S UTC') Hello world"
